\newday{26 February 2024}
	\action{Create a dataloader to dynamically load flux and psf}
	\action{Normalize electric fields, real and imaginary parts independently}
	\action{Normalize PL output fluxes}
	\action{Train a fully connected nn with horrible results, train mse keeps stable at 1, will have to look at the data}
\finishday